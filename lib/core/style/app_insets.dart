import 'package:chat/core/style/app_sizes.dart';
import 'package:flutter/rendering.dart';

class AppInsets {

  static const a4 = EdgeInsets.all(AppSizes.size4);
  static const a8 = EdgeInsets.all(AppSizes.size8);
  static const a16 = EdgeInsets.all(AppSizes.size16);

  static const h4 = EdgeInsets.symmetric(horizontal: AppSizes.size4);
  static const h8 = EdgeInsets.symmetric(horizontal: AppSizes.size8);
  static const h10 = EdgeInsets.symmetric(horizontal: AppSizes.size10);
  static const h12 = EdgeInsets.symmetric(horizontal: AppSizes.size12);
  static const h14 = EdgeInsets.symmetric(horizontal: AppSizes.size14);
  static const h16 = EdgeInsets.symmetric(horizontal: AppSizes.size16);
  static const h24 = EdgeInsets.symmetric(horizontal: AppSizes.size24);
  static const h32 = EdgeInsets.symmetric(horizontal: AppSizes.size32);

  static const v4 = EdgeInsets.symmetric(vertical: AppSizes.size4);
  static const v8 = EdgeInsets.symmetric(vertical: AppSizes.size8);
  static const v16 = EdgeInsets.symmetric(vertical: AppSizes.size16);

  static const hv4 = EdgeInsets.symmetric(horizontal: AppSizes.size4, vertical: AppSizes.size4);
  static const hv8 = EdgeInsets.symmetric(horizontal: AppSizes.size8, vertical: AppSizes.size8);
  static const hv16 = EdgeInsets.symmetric(horizontal: AppSizes.size16, vertical: AppSizes.size16);

}